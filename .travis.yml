sudo: false

language: python
python:
  - "2.7"

cache: pip

script:
  - ./manage.py check
  - py.test -k "not unoconv"
  - make shower

addons:
  apt:
    packages:
    - graphicsmagick
    # - unoconv
    - python-dev

notifications:
  email:
    on_success: change
    on_failure: change
