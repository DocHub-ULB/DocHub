language: python
python:
  - "2.7"

cache: pip

script:
  - ./manage.py check
  - py.test
  - make shower

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq  graphicsmagick unoconv python-dev
